# V5 故事節奏優化任務清單

> 基於 V2.0.0 (140 tests pass)，目標：讓自動剪輯的節奏感和故事感更接近人類剪輯師。

---

## 任務 1：Clip 時長連續跟隨能量曲線

**目的**：verse 段用長鏡頭呼吸，chorus 段切快碎鏡頭，節奏自然跟著音樂流動。

### 1.1 能量→時長連續映射函數
- [ ] 新增 `energy_clip_duration()` 方法到 `base.py`
- [ ] 公式：`clip_max = base_max * (1.2 - factor * energy)`
- [ ] 新增 `energy_clip_factor: float = 0.6` 到 `RhythmConfig`
- [ ] 替換現有的 step-based 能量邏輯

### 測試
- [ ] 驗證 energy=0 時 clip_max ≈ base_max * 1.2
- [ ] 驗證 energy=1 時 clip_max ≈ base_max * 0.6
- [ ] 確認 clip_min 下限保護

**關鍵檔案**：`planner/base.py`, `models.py`, `config/*.yaml`
**難度**：低 | **影響**：高

---

## 任務 2：高光主動鎖定 Chorus

**目的**：保證最精彩的畫面落在音樂最激動的位置，而非被動靠分數浮上來。

### 2.1 Chorus 時間窗預留機制
- [ ] 在 `plan()` 開頭掃描 chorus 區間
- [ ] 從全域 shot pool 取 top-N 高光 shot 預留給 chorus
- [ ] 已預留 shot 標記為 used，後續不再重複選用

### 2.2 Chorus 評分加權
- [ ] chorus 區間內，高光 shot 加分 (+0.25)
- [ ] chorus 區間外，預留 shot 扣分 (-0.10)

### 2.3 Chorus Repeat 多樣性
- [ ] 不同 chorus 強制使用不同事件的高光 shot
- [ ] 事件不足時 fallback 但記錄 warning

### 測試
- [ ] 驗證 chorus 區間確實放入高光 shot
- [ ] 驗證重複 chorus 使用不同事件來源
- [ ] 邊界：無音樂 / chorus 數量 0/1/多個

**關鍵檔案**：`planner/base.py`, `models.py`
**難度**：中 | **影響**：高

---

## 任務 3：章節情緒弧線分配

**目的**：每章有自己的情緒區間，讓整支影片呈現「緩→高潮→收」的故事弧線。

### 3.1 情緒範圍定義
- [ ] 各 planner 新增 `chapter_emotion_ranges()` 抽象方法
- [ ] Growth: opening(0-0.35), learning(0.25-0.65), highlights(0.55-1.0), closing(0.1-0.4)
- [ ] Travel: departure(0.1-0.4), exploration(0.3-0.7), interaction(0.5-1.0), ending(0-0.35)

### 3.2 情緒感知事件分配
- [ ] 計算每個事件的平均情緒值 (face + motion) / 2
- [ ] 按情緒範圍匹配事件到章節，取代簡單的 round-robin 分配

### 測試
- [ ] 驗證高情緒事件分配到 highlights/interaction
- [ ] 驗證低情緒事件分配到 opening/ending

**關鍵檔案**：`planner/base.py`, `planner/growth.py`, `planner/travel.py`
**難度**：中 | **影響**：高

---

## 任務 4：電影感首尾 (Cinematic Edges)

**目的**：開頭淡入、結尾淡出，營造電影感。

### 4.1 TransitionType 擴充
- [ ] 新增 `FADE_FROM_BLACK` 和 `FADE_TO_BLACK`

### 4.2 首尾轉場邏輯
- [ ] 第一個 clip：`transition_in = FADE_FROM_BLACK`，duration 1.0s
- [ ] 最後一個 clip：`transition_out = FADE_TO_BLACK`，duration 1.5s

### 4.3 Premiere XML 支援
- [ ] 新增 `_transition_effect_name()` 映射函數
- [ ] fade 類型映射到 `DipToColorDissolve`

### 測試
- [ ] 驗證首尾 clip 有正確轉場類型
- [ ] 驗證 XML 輸出正確的 effect ID

**關鍵檔案**：`models.py`, `planner/base.py`, `export/premiere_xml.py`
**難度**：低 | **影響**：中

---

## 任務 5：語意轉場 (Semantic Transitions)

**目的**：根據畫面內容自動選擇適當轉場，而非全部硬切。

### 5.1 Grammar 語意規則
- [ ] 同事件時間跳躍 >30s → cross dissolve（時間流逝感）
- [ ] 相鄰 clip 情緒對比 >0.5 → fade to/from black（情緒重置）
- [ ] 照片↔影片媒體切換 → cross dissolve（平滑過渡）

### 5.2 Grammar Report 擴充
- [ ] 新增 `semantic_transitions_added` 欄位
- [ ] 新增 `emotion_gradient_score` 欄位（0-1 情緒弧線吻合度）

### 測試
- [ ] 各場景觸發正確轉場類型
- [ ] chapter 邊界不被語意規則覆蓋

**關鍵檔案**：`planner/grammar.py`, `models.py`
**難度**：低 | **影響**：中

---

## 任務 6：Chorus Repeat 指紋偵測（進階）

**目的**：音樂重複的 chorus 段使用不同視覺內容，避免觀眾疲勞。

### 6.1 Chorus Chroma 指紋
- [ ] 在 `beat.py` 用 chroma cosine similarity 偵測重複 chorus
- [ ] `MusicSection` 新增 `repeat_index: int = 0`
- [ ] 相似度 > 0.85 歸為同組，各組內按出現順序編號

### 6.2 跨 Chorus 事件排斥強化
- [ ] 每次 chorus 排除所有前次 chorus 已用過的事件（非僅前一次）
- [ ] 事件不足時 fallback 但記錄 warning

### 測試
- [ ] 驗證 repeat_index 正確分配
- [ ] 驗證重複 chorus 使用不同事件

**關鍵檔案**：`analyze/beat.py`, `planner/base.py`, `models.py`
**難度**：中 | **影響**：中

---

## 任務 A：P0 穩定性與可執行性

**目的**：確保測試和 pipeline 在任何環境下穩定運行。

- [ ] pytest 設定 `tmp_path_retention_policy = "none"`
- [ ] 新增 smoke test：import pipeline/CLI、載入 config、dry-run、版本檢查
- [ ] 抑制第三方 logger (numba, librosa, matplotlib, PIL, urllib3, soundfile, audioread)

**關鍵檔案**：`pyproject.toml`, `cli.py`, `tests/test_smoke.py`
**難度**：低 | **影響**：高

---

## 任務 B：P0 DNG 素材可用性

**目的**：DNG 不會默默被忽略，報表可追蹤原因。

- [ ] 啟動時檢查 ffmpeg/exiftool 可用性
- [ ] exiftool 不可用時記錄 skip 原因
- [ ] 報表中列出被跳過的 DNG 與原因

**關鍵檔案**：`ingest/scanner.py`, `report/writer.py`, `pipeline.py`
**難度**：低 | **影響**：高

---

## 任務 C：P0 目標片長收斂

**目的**：最終片長控制在 target ±5% 以內。

- [ ] Backfill 門檻提升到 95%（原 85%）
- [ ] 新增 trim pass：超過 105% 時移除最低分 clip
- [ ] 保護章節最低保留比例：opening 60%, highlights 70%, closing 50%

**關鍵檔案**：`planner/base.py`
**難度**：中 | **影響**：高

---

## 任務 D：P1 情緒曲線寫入報表

**目的**：報表可視化章節情緒分布。

- [ ] `report.json` 新增 `chapter_energy` 欄位
- [ ] 每章：avg_emotion, peak_emotion, clip_count, highlight_count
- [ ] grammar 區段加入 `emotion_gradient_score`

**關鍵檔案**：`report/writer.py`
**難度**：低 | **影響**：中

---

## 任務 E：P1 音樂段落對齊加強

**目的**：chorus 更明確放入動態高光鏡頭，verse 更明確放入呼吸敘事鏡頭。

- [ ] chorus 評分：highlight 權重 0.3→0.4，action bonus 0.3→0.35
- [ ] chorus 額外加分：highlight >= 0.6 時 +0.15
- [ ] verse 懲罰：motion > 0.7 時 -0.1

**關鍵檔案**：`planner/base.py`
**難度**：低 | **影響**：中

---

## 任務 F：P1 Grammar 2.0

**目的**：降低連續性問題，提升轉場品質。

- [ ] 連續同角色 swap 搜索範圍擴大到 i+8（原 i+6）
- [ ] 新增 `_compute_emotion_gradient()` 量化情緒弧線吻合度

**關鍵檔案**：`planner/grammar.py`
**難度**：低 | **影響**：中

---

## 任務 G：P2 日誌與報表可讀化

**目的**：報表加入 KPI 摘要，方便快速評估品質。

- [ ] 報表頂層加入 `kpi_summary`：clips數、片長、事件覆蓋率、高光率、beat對齊率等
- [ ] grammar 區段加入 `semantic_transitions_added`

**關鍵檔案**：`report/writer.py`
**難度**：低 | **影響**：低

---

## 建議開發順序

```
任務 1（能量→時長）→ A/B/C（三個 P0）→ 任務 2、3（chorus + 情緒）
  → D/E/F（報表 + 對齊 + grammar）→ 任務 4、5（轉場）→ 任務 6 + G（進階 + 收尾）
```

## 驗收標準

1. 所有新增測試 + 原有 140 tests 全部通過
2. `uv run roughcut run` 仍可正常執行
3. 有音樂時，chorus 段明顯放入高光鏡頭
4. 整體 timeline 呈現「緩→快→緩」的節奏弧線
5. 首尾有電影感的淡入淡出
6. 報表包含 KPI 摘要、章節能量、語意轉場統計
